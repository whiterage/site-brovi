# 📋 ЧЕК-ЛИСТ ДЛЯ РАЗРАБОТКИ САЙТОВ

> Памятка для фуллстек разработчика по верстке и деплою проектов

---

## 🚀 0. НАСТРОЙКА VITE И ЛОКАЛЬНОГО ОКРУЖЕНИЯ

### Инициализация проекта

```bash
# Если проект новый
npm init -y
npm install vite --save-dev

# Если уже есть package.json
npm install
```

### Структура проекта

```
frontend/
├── index.html          # Точка входа (должен быть в корне frontend)
├── src/                # Все исходники
│   ├── main.js
│   ├── styles/
│   └── components/
├── public/             # Статика: изображения, иконки
├── package.json
└── vite.config.js      # Опционально, для кастомных настроек
```

### Настройка package.json

Добавь скрипты:

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

### Запуск локального сервера

```bash
npm run dev
```

- Vite запустит сервер на `http://localhost:5173` (или другой свободный порт)
- Автоматическая перезагрузка при изменении файлов

### Важные моменты

- ✅ `index.html` должен быть в корне `frontend` (не в `src`)
- ✅ В `index.html` подключай JS через `<script type="module" src="/src/main.js">`
- ✅ CSS можно импортировать в JS: `import './styles/main.css'`
- ✅ Vite автоматически обрабатывает импорты и оптимизирует сборку
- ✅ Для статики используй `/public/` - файлы доступны по корневому пути (`/images/logo.png`)

### Кастомные настройки (опционально)

Создай `vite.config.js`:

```javascript
export default {
  server: {
    port: 3000,
    open: true
  },
  resolve: {
    alias: {
      '@': '/src'
    }
  }
}
```

### Проверка

- [ ] Открой `http://localhost:5173`
- [ ] Должна открыться страница
- [ ] Измени файл - страница должна обновиться автоматически
- [ ] В консоли браузера не должно быть ошибок

---

## 📐 1. ПЛАНИРОВАНИЕ И АРХИТЕКТУРА

Перед началом кода:

- [ ] Определить все страницы и их функционал
- [ ] Спроектировать структуру компонентов (что переиспользуется)
- [ ] Выбрать подход к стилям (CSS модули, BEM, utility-first)
- [ ] Определить breakpoints (мобилка, планшет, десктоп)
- [ ] Согласовать с дизайнером: есть ли макеты для всех разрешений

---

## 📱 2. MOBILE-FIRST ПОДХОД (ОБЯЗАТЕЛЬНО)

**Правило:** начинай с мобильной версии, потом расширяй до планшета и десктопа.

### Почему?

- ✅ Проще расширять, чем сжимать
- ✅ Меньше кода и конфликтов
- ✅ Лучше производительность на мобильных

### Как?

1. Пиши базовые стили для мобилки (до 767px)
2. Потом добавляй `@media (min-width: 768px)` для планшета
3. Потом `@media (min-width: 1025px)` для десктопа
4. ❌ НЕ используй фиксированные размеры (px) для контейнеров, отступов, шрифтов

---

## 🚫 3. ЗАПРЕТ НА ФИКСИРОВАННЫЕ РАЗМЕРЫ

### ❌ Что НЕЛЬЗЯ:

```css
width: 500px;           /* только max-width или процент/флекс */
height: 300px;          /* только min-height или auto */
font-size: 16px;        /* используй rem/em или clamp */
margin: 20px;           /* используй rem или проценты */
padding: 15px;          /* используй rem или проценты */
```

### ✅ Что МОЖНО:

```css
max-width: 1280px;                           /* для контейнеров */
min-height: 100vh;                           /* для секций */
font-size: clamp(14px, 2vw, 18px);          /* адаптивный шрифт */
padding: 1rem 2rem;                          /* относительные единицы */
gap: 1rem;                                   /* во flex/grid */
```

---

## 📁 4. СТРУКТУРА CSS ФАЙЛОВ

Организуй так:

```
styles/
├── base.css           # Сброс, переменные, базовые стили
├── components.css     # Кнопки, карточки, формы - переиспользуемые
├── layout.css         # Header, footer, grid системы
├── page-specific.css  # Стили для конкретных страниц
├── mobile.css         # Только мобильные переопределения
└── tablet.css         # Только планшетные переопределения
```

**Правило:** если стиль повторяется на 2+ страницах - он в `components.css`

---

## 🎨 5. CSS ПЕРЕМЕННЫЕ (ОБЯЗАТЕЛЬНО)

Создай `tokens.css` с переменными:

- Цвета (основные, акцентные, текст)
- Шрифты (семейства, размеры)
- Отступы (spacing scale)
- Breakpoints
- Тени, радиусы, переходы

**Почему:** легко менять дизайн, поддерживать консистентность

---

## 🧩 6. КОМПОНЕНТНЫЙ ПОДХОД

Перед версткой компонента:

- [ ] Это переиспользуемый компонент? → выноси в отдельный файл/класс
- [ ] Есть состояния (hover, active, disabled)? → продумай все
- [ ] Нужна анимация? → добавь сразу, не потом
- [ ] Работает на всех разрешениях? → проверь сразу

---

## 🧪 7. ТЕСТИРОВАНИЕ НА КАЖДОМ ЭТАПЕ

После каждого компонента/страницы:

- [ ] Открой DevTools, проверь мобилку (375px, 414px)
- [ ] Проверь планшет (768px, 1024px)
- [ ] Проверь десктоп (1280px, 1920px)
- [ ] Проверь очень широкий экран (2560px+)
- [ ] Проверь очень узкий (320px)

**Используй:** Device Toolbar в Chrome DevTools

---

## 🔄 8. ИЗБЕГАЙ ДУБЛИРОВАНИЯ КОДА

**Правило трех:** если код повторяется 3 раза - вынеси в функцию/компонент/миксин

### Примеры:

- Кнопки с одинаковым стилем → один класс `.btn`
- Карточки товаров → функция `createCard()`
- Модальные окна → компонент `Modal`
- Формы → переиспользуемые компоненты

---

## ⚡ 9. ПРОИЗВОДИТЕЛЬНОСТЬ

С самого начала:

- [ ] Оптимизируй изображения (WebP, сжатие)
- [ ] Используй lazy loading для изображений
- [ ] Минимизируй количество HTTP запросов
- [ ] Используй CSS transitions вместо JS анимаций где возможно
- [ ] Не загружай тяжелые библиотеки без необходимости

---

## 👨‍🎨 10. РАБОТА С ДИЗАЙНЕРОМ

### Вопросы перед началом:

- [ ] Есть ли макеты для мобилки/планшета/десктопа?
- [ ] Какие breakpoints использовать?
- [ ] Какие шрифты и где брать?
- [ ] Есть ли дизайн-система/стильгайд?
- [ ] Как обрабатывать edge cases (очень длинный текст, пустые состояния)?

### Во время разработки:

- [ ] Показывай промежуточные результаты
- [ ] Спрашивай про неясные моменты сразу
- [ ] Документируй решения, которые отличаются от макета

---

## ⚠️ 11. ЧАСТЫЕ ОШИБКИ (КОТОРЫХ ИЗБЕГАТЬ)

- ❌ Фиксированные размеры вместо относительных
- ❌ Верстка только для десктопа, потом "допиливание" мобилки
- ❌ Использование `!important` без крайней необходимости
- ❌ Дублирование стилей вместо переиспользования
- ❌ Забывание про touch-события на мобилке
- ❌ Игнорирование accessibility (a11y)
- ❌ Неоптимизированные изображения
- ❌ Жестко заданные z-index значения без системы

---

## ✅ 12. ЧЕК-ЛИСТ ПЕРЕД СДАЧЕЙ

- [ ] Все страницы работают на мобилке/планшете/десктопе
- [ ] Нет горизонтального скролла на мобилке
- [ ] Все интерактивные элементы кликабельны (минимум 44x44px на мобилке)
- [ ] Шрифты читаемы на всех устройствах
- [ ] Изображения не "ломают" layout при загрузке
- [ ] Формы работают на всех устройствах
- [ ] Нет консольных ошибок
- [ ] Производительность хорошая (Lighthouse score > 90)
- [ ] Код чистый, без дублирования
- [ ] Все стили организованы логично

---

## 🛠️ 13. ИНСТРУМЕНТЫ ДЛЯ УСКОРЕНИЯ

- CSS Grid и Flexbox для layout (не float/position везде)
- `clamp()` для адаптивных размеров
- CSS переменные для темизации
- Автопрефиксер для кроссбраузерности
- Линтеры (ESLint, Stylelint) с самого начала
- Git: коммить часто, понятные сообщения

---

## 💭 14. МЕНТАЛЬНАЯ МОДЕЛЬ

Помни:

- Верстка = решение проблем адаптивности, а не просто "сделать как в макете"
- Мобилка не "урезанная версия десктопа", а полноценный опыт
- Код пишется один раз, но читается много раз - делай его понятным
- Лучше потратить время на архитектуру в начале, чем переделывать потом

---

## 🚢 15. ДЕПЛОЙ НА СЕРВЕР

### Подготовка к деплою

#### 1. Сборка production версии

```bash
npm run build
```

- Vite создаст папку `dist/` с оптимизированными файлами
- Проверь `dist/` - там должны быть HTML, JS, CSS, изображения

#### 2. Создание Dockerfile

- Используй nginx для раздачи статики
- Базовый Dockerfile: `FROM nginx`, `COPY dist/` в `/usr/share/nginx/html`
- Настрой `nginx.conf` для SPA (если нужно) - все роуты на `index.html`

#### 3. Создание docker-compose.yml

- Определи сервисы (frontend, backend если есть)
- Настрой volumes для статики
- Настрой networks для связи между контейнерами
- Добавь environment variables если нужны
- Настрой `restart: always` для автоперезапуска

#### 4. Настройка контейнеров

- **Frontend контейнер:** nginx с собранными файлами
- **Backend контейнер** (если есть): Node.js/Python/etc с API
- **Database контейнер** (если нужна): PostgreSQL/MySQL/MongoDB
- **Прокси контейнер** (опционально): nginx для роутинга между frontend/backend

#### 5. Покупка и настройка домена

- Купи домен у регистратора
- Зайди в панель управления DNS
- Создай A-запись: `@` → IP твоего сервера
- Создай A-запись: `www` → IP твоего сервера (опционально)
- Подожди распространения DNS (обычно 5-30 минут, иногда до 24 часов)

#### 6. Подготовка сервера

- Установи Docker и Docker Compose на сервер
- Склонируй репозиторий на сервер (или залей файлы через scp/sftp)
- Убедись что порты открыты в firewall (80, 443, и порты для контейнеров)

#### 7. Поднятие контейнеров на сервере

```bash
# Зайди на сервер по SSH
ssh user@your-server

# Перейди в папку с проектом
cd /path/to/project

# Запуск в фоне
docker-compose up -d

# Проверка что все контейнеры запущены
docker-compose ps

# Просмотр логов для проверки
docker-compose logs
```

#### 8. Настройка SSL сертификата (HTTPS)

- Используй Let's Encrypt через certbot
- Или настрой автоматический SSL в docker-compose через nginx-proxy
- Обнови `nginx.conf` для редиректа HTTP → HTTPS

#### 9. Проверка и тестирование

- [ ] Открой домен в браузере - должен открыться сайт
- [ ] Проверь все страницы работают
- [ ] Проверь на мобилке/планшете/десктопе
- [ ] Проверь что HTTPS работает
- [ ] Проверь что формы отправляются (если есть)
- [ ] Проверь что API запросы работают (если есть backend)

#### 10. Мониторинг и логи

```bash
# Следить за логами в реальном времени
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs frontend

# Последние 100 строк
docker-compose logs --tail=100

# Следи за использованием ресурсов
docker stats
```

- Настрой логирование ошибок в приложение

#### 11. Обновление сайта

```bash
# Внеси изменения в код локально
# Собери новую версию
npm run build

# Залей изменения на сервер (git pull или scp)
git pull  # или scp -r dist/ user@server:/path/to/project/

# Останови контейнеры
docker-compose down

# Пересобери образы если нужно
docker-compose build

# Запусти заново
docker-compose up -d

# Или просто перезапуск (если не менялся Dockerfile)
docker-compose restart
```

#### 12. Резервное копирование

- [ ] Настрой автоматический бэкап базы данных (если есть)
- [ ] Делай бэкап конфигов docker-compose и nginx
- [ ] Храни копии собранных `dist/` на случай отката

### Типичные проблемы при деплое

| Проблема | Решение |
|----------|---------|
| Порт уже занят | Проверь `docker-compose ps` и освободи порт |
| Файлы не найдены | Проверь пути в Dockerfile и nginx.conf |
| DNS не резолвится | Подожди распространения или проверь настройки |
| SSL не работает | Проверь что порт 443 открыт и certbot настроен |
| Контейнер падает | Смотри логи: `docker-compose logs` |
| Сайт не обновляется | Очисти кеш браузера или сделай hard refresh |

### Полезные команды для деплоя

```bash
docker-compose up -d      # Запуск
docker-compose down       # Остановка
docker-compose restart    # Перезапуск
docker-compose ps         # Статус
docker-compose logs -f    # Логи
docker-compose build      # Пересборка
docker-compose pull       # Обновление образов
```

---

## 🎯 БЫСТРЫЙ СТАРТ ПЕРЕД КАЖДЫМ ПРОЕКТОМ

1. ✅ **Настрой Vite:** `npm install`, `npm run dev`, проверь что работает
2. ✅ **Спроси у дизайнера:** есть макеты для всех разрешений?
3. ✅ **Настрой структуру файлов** (base, components, layout, pages)
4. ✅ **Создай tokens.css** с переменными
5. ✅ **Начни верстать с мобильной версии**
6. ✅ **Используй только относительные единицы** (rem, %, vw, vh, clamp)
7. ✅ **Тестируй на каждом этапе**
8. ✅ **Выноси повторяющийся код в компоненты**
9. ✅ **Перед сдачей - прогони через чек-лист**
10. ✅ **Деплой:** build → docker-compose → домен → DNS → поднятие → проверка → логи

---

**Удачи в разработке! 🚀**
