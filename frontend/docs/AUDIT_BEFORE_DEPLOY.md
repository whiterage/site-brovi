# Аудит перед деплоем

## ✅ Исправлено в рамках аудита

### Критично для сборки
- **Папки `aeth/` и `price/`** — использовались в `index.html` по путям `/aeth/` и `/price/`, но лежали в корне `frontend/`, а не в `public/`. При сборке Vite копирует в `dist` только содержимое `public/`, поэтому картинки секций «Эстетика» и «Прайс лист (ресницы)» не попадали в прод. **Исправлено:** папки скопированы в `frontend/public/`. После `npm run build` в `dist` есть `aeth/` и `price/`.

---

## ✅ Уже в порядке

### Сборка и конфиг
- **Vite multi-page** — в сборку входят все 5 страниц: `index`, `obuchenie`, `brovi`, `resnitsy`, `podval`.
- **Скрипты** — `npm run build`, `npm run dev`, `npm run preview` работают.
- **.gitignore** — в репозиторий не попадают `node_modules/`, `dist/`, `.env*`.

### Безопасность и ссылки
- Все ссылки с `target="_blank"` имеют `rel="noopener noreferrer"` (WhatsApp, Telegram, Instagram, Yandex Maps, yclients).
- Кнопка «Меню» в шапке — `href="#"` и открытие панели через JS, это ожидаемо.

### Доступность и базовая разметка
- У кнопок и ссылок есть `aria-label` где нужно.
- У панели навигации — `aria-hidden`, у диалога полноэкранного просмотра — `role="dialog"`, `aria-modal`, `aria-label`.
- У страниц указан `lang="ru"`, у изображений — `alt=""` (декоративные) или осмысленный `alt`.

### Производительность и UX
- Критический inline-CSS в `<head>`: белый фон, скрытие body и `.nav-panel` до загрузки стилей.
- Плавное появление: после загрузки стилей добавляется класс `is-ready`, body показывается с transition.
- Preload LCP-картинки на главной (`/Frame%2011.png`), `fetchpriority="high"` у героя.
- У изображений ниже сгиба — `loading="lazy"` и `decoding="async"` где нужно.
- Шрифт Onest подключается с `display=swap`.

### Контент
- Заголовок главной — «Приймак Алина — обучение, перманентный макияж».
- У остальных страниц осмысленные `<title>`.

---

## ⚠️ Рекомендации перед/после деплоя

### 1. SEO и соцсети
- **Сделано:** на все 5 страниц добавлен `<meta name="description" content="...">` с кратким описанием для поисковиков.
- При желании можно добавить Open Graph: `og:title`, `og:description`, `og:image`.

### 2. Деплой и чистые URL
- **Корень сайта** — при деплое отдавать содержимое **`frontend/dist`** как корень сайта.
- **Чистые URL без .html** настроены:
  - **Netlify:** в `frontend/public/_redirects` прописаны правила (копируются в dist). Запросы `/obuchenie`, `/brovi`, `/resnitsy`, `/podval` отдают соответствующий `.html`, URL в браузере остаётся без расширения.
  - **Vercel:** в корне проекта `vercel.json` с `rewrites` и `outputDirectory: "frontend/dist"`.
- Все внутренние ссылки переведены на чистые пути: `/`, `/obuchenie`, `/brovi`, `/resnitsy`, `/podval` и якоря `/#alina`, `/#feedback` и т.д.
- **Base URL** — если сайт будет не в корне домена (например `https://example.com/site/`), в Vite задать `base: '/site/'` и пересобрать.

### 3. Дублирование ассетов
- Сейчас есть и **`frontend/aeth/`**, **`frontend/price/`**, и их копии в **`frontend/public/aeth/`**, **`frontend/public/price/`**. Для деплоя используется только `public/`. Старые папки в корне `frontend/` можно удалить, чтобы не путаться (предварительно убедившись, что везде используются пути от корня сайта `/aeth/`, `/price/`).

### 4. Тестирование перед выкладкой
- Прогнать `npm run build` и `npm run preview`, пройти все страницы и проверить:
  - открытие меню, переходы по ссылкам, кнопки (WhatsApp, запись и т.д.);
  - загрузку всех картинок (главная, обучение, брови, ресницы, подвал);
  - полноэкранный просмотр фото на podval;
  - работу на телефоне (тап, скролл, нет горизонтальной полосы).
- Проверить в DevTools вкладку Network: нет ли 404 на JS/CSS/картинки.

### 5. Мелкие улучшения (не блокеры)
- **Пустой `page-specific.css`** — файл есть, но не импортируется (импорт убран при оптимизации). Можно удалить файл или вернуть импорт, если планируется контент.
- **Ссылка «Обучение» в меню** — ведёт на `obuchenie.html`. Если позже появятся «красивые» URL (`/obuchenie`), ссылки в меню нужно будет обновить под них.

---

## 📋 Чек-лист перед деплоем

- [x] `npm run build` выполняется без ошибок
- [x] В `dist` есть все 5 HTML, папки `assets/`, `aeth/`, `price/`, `alina/`, `lookall/`, `mac/`, `test/`, `buttons/`, `navi/` и остальные файлы из `public/`
- [ ] На хостинге настроена раздача статики из `dist` и при необходимости маршрутизация для `.html` или чистых URL
- [ ] При необходимости задан `base` в Vite и пересобран проект
- [ ] Проверены все страницы и ссылки после деплоя (в т.ч. с телефона)
- [ ] При желании добавлены meta description и Open Graph

После выполнения пунктов чек-листа и рекомендаций по хостингу проект готов к продакшен-деплою.
